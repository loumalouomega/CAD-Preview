% Toolbar icon: circle — a plain unfilled circle (the Markup circle tool). Distinct from
% point.tex, which adds a filled centre dot.
\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{arrows.meta,calc,shapes.geometric}
\begin{document}
\begin{tikzpicture}[line width=1.3pt, line cap=round, line join=round, >=Stealth, x=1mm,y=1mm]
  \draw[thick] (0,0) circle (8.8);
\end{tikzpicture}
\end{document}
