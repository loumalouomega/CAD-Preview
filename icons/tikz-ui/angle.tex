% Toolbar icon: angle — two rays from a common vertex with an arc spanning between them.
\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{arrows.meta,calc,shapes.geometric}
\begin{document}
\begin{tikzpicture}[line width=1.3pt, line cap=round, line join=round, >=Stealth, x=1mm,y=1mm]
  \draw[thick] (-8,-7) -- (9.5,-7);
  \draw[thick] (-8,-7) -- (5.5,8.5);
  \draw[thick] (-8,-7) ++(0:8) arc (0:49:8);
\end{tikzpicture}
\end{document}
