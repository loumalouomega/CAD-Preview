% Op icon: wrap — upright cylinder with a wider shaded band wrapped around
% its middle (the developed shell hugging the target surface).
% NOTE: built without pdflatex (not installed in this environment); the
% committed icons/svg-ops/wrap.svg is hand-authored in pdftocairo -svg output
% style so `node build-op-icons.mjs` processes it identically. Re-render from
% this source with `make svg-ops/wrap.svg` when a TeX install is available and
% diff the result against the committed SVG.
\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{arrows.meta,calc,shapes.geometric}
\begin{document}
\begin{tikzpicture}[line width=1.3pt, line cap=round, line join=round, >=Stealth, x=1mm,y=1mm]
  \draw[thick, fill=gray!10] (-12,-2) rectangle (12,18);
  \draw[thick, fill=gray!12] (0,18) ellipse (12 and 4.5);
  \draw[thick, fill=gray!25] (-15,2) rectangle (15,9);
\end{tikzpicture}
\end{document}
