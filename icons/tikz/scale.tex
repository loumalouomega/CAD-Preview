% Op icon: scale — small square growing into a larger dashed square, corner arrows.
\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{arrows.meta,calc,shapes.geometric}
\begin{document}
\begin{tikzpicture}[line width=1.3pt, line cap=round, line join=round, >=Stealth, x=1mm,y=1mm]
  \draw[dashed, gray] (-10,-10) rectangle (10,10);
  \draw[thick, fill=gray!20] (-5,-5) rectangle (5,5);
  \draw[->, thick] (5,5) -- (10,10);
  \draw[->, thick] (-5,-5) -- (-10,-10);
\end{tikzpicture}
\end{document}
