% Op icon: boolean intersect — only the lens-shaped overlap filled; both circles dashed.
\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{arrows.meta,calc,shapes.geometric}
\begin{document}
\begin{tikzpicture}[line width=1.3pt, line cap=round, line join=round, >=Stealth, x=1mm,y=1mm]
  \begin{scope}
    \clip (-3,0) circle (7);
    \fill[gray!25] (3,0) circle (7);
  \end{scope}
  \draw[dashed] (-3,0) circle (7);
  \draw[dashed] (3,0) circle (7);
\end{tikzpicture}
\end{document}
