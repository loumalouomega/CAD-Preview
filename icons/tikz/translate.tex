% Op icon: translate (Move) — dashed original position, solid new position, arrow between.
\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{arrows.meta,calc,shapes.geometric}
\begin{document}
\begin{tikzpicture}[line width=1.3pt, line cap=round, line join=round, >=Stealth, x=1mm,y=1mm]
  \draw[dashed, gray] (-10,-10) rectangle (0,0);
  \draw[thick, fill=gray!20] (0,0) rectangle (10,10);
  \draw[->, thick] (-5,-5) -- (5,5);
\end{tikzpicture}
\end{document}
